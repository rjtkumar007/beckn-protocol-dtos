# Beckn Protocol Schema Classes

Provides Beckn Protocol Schema classes for following platforms:
1. JVM

### Installation for JVM
1. Install JDK 11
2. Inside `jvm` directory run `./gradlew clean autoVersion build publishToMavenLocal`
